{
  "name": "ayni-presentacion",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:responsive": "npm run build && playwright test tests/responsive-layout.spec.ts tests/reveal-video.spec.ts",
    "test:unit": "playwright test --config playwright.unit.config.ts",
    "test": "npm run test:unit && npm run test:responsive"
  },
  "dependencies": {
    "@vite-pwa/astro": "^1.2.0",
    "animejs": "^4.5.0",
    "astro": "^7.0.0",
    "workbox-window": "^7.4.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
